package se.jbee.ipo;

public interface Projection {

	// Sometimes a process is more used as a internal tool and we want use and keep types existing.
	// Still a value like a Arg should be "serialized" as seperate (basic/atomic) set of attributes each
	// so it would be nice that we can add a transformation from a complex value to a set of attributes
}
