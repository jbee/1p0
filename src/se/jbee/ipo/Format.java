package se.jbee.ipo;

public class Format {

	//options: is it better to have another form when other options are used ? should options be part of the form ?

	// how to model ranges ? e.g. 1-10000 or length of a string 
	// options are a constraint as well but how can we make them understandable in a generic way ? 
	// all those are restrictions -> maybe that is the general concept below
}
